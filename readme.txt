You need to install the dependencies for hte script to work!

- Windows -

  Open cmd
  
  cd into the scripts folder

  and run the command below:
  pip install -r requirements.txt

  (OBS) you need to have pip and python installed!

- Linux -

  Open Terminal
   
  cd to script directory

  and run the command below:
  pip install -r requirements.txt
